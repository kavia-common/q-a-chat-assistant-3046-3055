import { Routes } from '@angular/router';
import { AppComponent } from './app.component';

export const routes: Routes = [
  // In a larger app we'd have separate pages; keeping placeholder route for completeness.
];
